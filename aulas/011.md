<div align="center">
    <a href="https://github.com/monicaquintal" target="_blank">
        <img src="https://cdn.jsdelivr.net/gh/devicons/devicon@latest/icons/laravel/laravel-original.svg" width="80px" align="left"/>  
    </a> 
    <h1>Aula 11: Autenticação WEB (Session) e Bootstrap (CSS)</h1>
    <br><br>
</div>

<a href="#topico1">1. Iniciando o projeto Controle de Tarefas</a><br>
<a href="#topico2">2. [LINUX] Instalando o NodeJS e o NPM</a><br>
<a href="#topico3">3. Instalando o Laravel UI</a><br>
<a href="#topico4">4. Entendendo o pacote UI e iniciando a autenticação WEB nativa do Laravel</a><br>
<a href="#topico5">5. Configurando a conexão com o banco de dados e executando as migrations padrões</a><br>
<a href="#topico6">6. Registrando novos usuários e efetuando o login na aplicação</a><br>
<a href="#topico7">7. Validando campos de confirmação (confirmed) e mudando regras de senhas</a><br>
<a href="#topico8">8. Entendendo como a rota Home está protegida</a><br>
<a href="#topico9">9. Criando o Model e o Controller para Tarefa</a><br>
<a href="#topico10">10. Implementando o middleware auth</a><br>
<a href="#topico11">11. Verificando se o usuário está logado dentro dos métodos do Controller</a><br>

<h2 id="topico1">1. Iniciando o projeto Controle de Tarefas</h2>

- na linha de comando, executar `composer create-project --prefer-dist laravel/laravel app_controle_tarefas`, e depois `php artisan server`

<h2 id="topico2">2. [LINUX] Instalando o NodeJS e o NPM</h2>

- são recursos importantes pois nessa seção trabalharemos com o recurso de pacote UI do Laravel.
    - `node.js` é uma plataforma de execução de código javascript; através dele nós temos condições de criar backends de aplicações web utilizando a linguagem de programação javascript. Também permite criar aplicações frontend baseadas no framework, como angular, react e vue, que são frameworks frontend muito utilizados no mercado.
    - `npm` é o gerenciador de pacotes do node. Ele é para o javascript o que o composer é para o php. Por meio do npm temos condições de adicionar dependências aos nossos projetos.

- para fazer a instalação, acessar o site do [Node.js](https://nodejs.org).
- baixar a versão LTS.

~~~bash
# Download and install nvm:
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.40.7/install.sh | bash
# in lieu of restarting the shell
\. "$HOME/.nvm/nvm.sh"
# Download and install Node.js:
nvm install 24
# Verify the Node.js version:
node -v # Should print "v24.19.0".
# Verify npm version:
npm -v # Should print "11.17.0".
~~~

~~~bash
nvm install 24
nvm alias default 24

node -v
npm -v
which node
~~~

- o npm é instalado durante esse procedimento.

<h2 id="topico3">3. Instalando o Laravel UI</h2>

- no diretório do projeto (`app_controle_tarefas`), executar o comando: 

~~~bash
composer require laravel/ui
composer show laravel/ui
~~~

<h2 id="topico4">4. Entendendo o pacote UI e iniciando a autenticação WEB nativa do Laravel</h2>

### a) Entendendo o pacote UI:

- esse pacote permite iniciar projetos com predefinições de frontend, que podem ser baseadas nos frameworks JS react ou vue e no kit css bootstrap.
- podemos ainda, por meio das APIs, trabalhar com outros frameworks, como angular.
- na linha de comando, utilizar:

~~~bash
php artisan ui <front_end> [auth]
php artisan ui bootstrap --auth
~~~

- esse comando produz um `Scaffold` (esqueleto) de uma aplicação: uma aplicação inicial, para que a partir dela possamos desenvolver o restante da aplicação.
- são insluídos diversos arquivos, como:
    - package.json: dependências javascript
    - routes/web.php: adiciona rotas de autenticação
    - app/Http/Controllers -> Auth
        - ConfirmPasswordController.php
        - ForgotPasswordController.php
        - LoginController.php
        - RegisterController.php
        - ResetController.php
        - VerificationController.php
    - app/models/User.php
    - public
        - css -> app.css
        - js -> app.js
    - resources/views 
        - home.blade.php
        - auth
            - login.blaade.php
            - register.blade.php
            - verify.blade.php
            - passwords (confirm.blade.php, email.blade.php. reset.blade.php)
        - layouts
            - app.blade.php
    - resources/js/bootstrap.js: pré-definições do JS de acordo com o frontend selecionado

### b) Preparando o projeto app_controle_tarefas:

- executar o comando:

~~~bash
php artisan ui bootstrap --auth
npm install
npm run dev
~~~

<h2 id="topico5">5. Configurando a conexão com o banco de dados e executando as migrations padrões</h2>

- no workbench: `create database ct;`.
- no arquivo .env, na raiz do projeto, configurar as variáveis de conexão:

~~~bash
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=ct
DB_USERNAME=root
DB_PASSWORD=senha
~~~

- executar `php artisan migrate`.