<div align="center">
    <a href="https://github.com/monicaquintal" target="_blank">
        <img src="https://cdn.jsdelivr.net/gh/devicons/devicon@latest/icons/laravel/laravel-original.svg" width="80px" align="left"/>  
    </a> 
    <h1>Aula 07: Trabalhando com formulários</h1>
    <br><br>
</div>

<a href="#topico1">1. Entendendo o objeto Request</a><br>
<a href="#topico2">2. Gravando os dados do formulário no banco de dados</a><br>
<a href="#topico3">3. Validação de campos obrigatórios (required)</a><br>
<a href="#topico4">4. Validação de quantidades mínimas e máximas de caracteres (min e max)</a><br>
<a href="#topico5">5. Repopulando o formulário (Request Old Input) parte 1</a><br>
<a href="#topico6">6. Repopulando o formulário (Request Old Input) parte 2</a><br>
<a href="#topico7">7. Ajustando o formulário de contato na rota principal</a><br>
<a href="#topico8">8. Refactoring do projeto Super Gestão parte 1</a><br>
<a href="#topico9">9. Refactoring do projeto Super Gestão parte 2</a><br>
<a href="#topico10">10. Validação de campos e-mail</a><br>
<a href="#topico11">11. Persistindo dados e redirecionando a rota</a><br>
<a href="#topico12">12. Validação de campos únicos (unique)</a><br>
<a href="#topico13">13. Customizando a apresentação dos erros de validação parte 1</a><br>
<a href="#topico14">14. Customizando a apresentação dos erros de validação parte 2</a><br>
<a href="#topico15">15. Customizando as mensagens de feedback de validação</a><br>

<h2 id="topico1">1. Entendendo o objeto Request</h2>

- `Request`: requisição que é feita por um cliente (um browser) para o backend da aplicação.
    - sempre que uma requisição é feita, como uma ação de envio de formuilário, de consulta de uma página ou de conexão com API, por exemplo, temos um Request.

- no terminal, utilizar o comando `php artisan serve`.
- acessar a **rota Contato**.
    - Inspecionar > aba Network exibe os detalhes da requisição.
    - o próprio framework Laravel tem inteligência para recuperar o Request e transformar em objeto, que será recebido no Controlador como um parâmetro, dentro do qual poderemos manipular de acordo com a necessidade.
- em `ContatoController.php`:

~~~php
<?php

namespace App\Http\Controllers;

use Illuminate\Http\Request;

class ContatoController extends Controller
{
    public function contato(Request $request) {
        dd($request);
        return view('site.contato');
    }
}
~~~

- como retorno, obteremos:

~~~
Illuminate\Http\Request {#60 ▼ // app/Http/Controllers/ContatoController.php:10
  +attributes: 
Symfony\Component\HttpFoundation
\
ParameterBag
 {#65 ▶}
  +request: 
Symfony\Component\HttpFoundation
\
InputBag
 {#61 ▼
    #parameters: []
  }
  +query: 
Symfony\Component\HttpFoundation
\
InputBag
 {#68 ▶}
  +server: 
Symfony\Component\HttpFoundation
\
ServerBag
 {#63 ▶}
  +files: 
Symfony\Component\HttpFoundation
\
FileBag
 {#67 ▶}
  +cookies: 
Symfony\Component\HttpFoundation
\
InputBag
 {#66 ▶}
  +headers: 
Symfony\Component\HttpFoundation
\
HeaderBag
 {#62 ▶}
  #content: null
  #languages: null
  #charsets: null
  #encodings: null
  #acceptableContentTypes: null
  #pathInfo: "/contato"
  #requestUri: "/contato"
  #baseUrl: ""
  #basePath: null
  #method: "GET"
  #format: null
  #session: 
Illuminate\Session
\
SymfonySessionDecorator
 {#273 ▶}
  #locale: null
  #defaultLocale: "en"
  -preferredFormat: null
  -isHostValid: true
  -isForwardedValid: true
  -isSafeContentPreferred: ? bool
  -trustedValuesCache: []
  -isIisRewrite: false
  #json: null
  #convertedFiles: null
  #userResolver: Closure($guard = null) {#232 ▶}
  #routeResolver: Closure() {#244 ▶}
  #cachedAcceptHeader: null
  basePath: ""
  format: "html"
}
~~~

- sendo que no atributo `request` podemos listar todos os parâmetros que foram encaminhados a um determinado formulário. Para testarmos:
    - comentar a instrução `dd($request);` em `ContatoController.php`.
    - acessar novamente a rota contato.
    - a ideia é enviar o formulário , de modo que os parâmetros sejam recuperados através do objeto Request.
    - portanto, preencher os dados do formulário Contato.
    - antes de clicar em Enviar, descomentar `dd($request);` em `ContatoController.php`.
    - dentro do atributo Request, recuperaremos as informações do formulário, como no exemplo:

~~~
  +request: 
Symfony\Component\HttpFoundation
\
InputBag
 {#61 ▼
    #parameters: array:6 [▼
      "_token" => "Ist2jy0S8KjHkq70cn1Xrz8gyQPrmnBopVfEtl3c"
      "nome" => "Monica"
      "telefone" => "11999999999"
      "email" => "monica@teste.com"
      "motivo_contato" => "2"
      "mensagem" => "Oi!"
    ]
  }
~~~

- em `ContatoController.php` podemos incluir o **método all()**, que recupera o array associativo de parâmetros do Request:

~~~php
<?php

namespace App\Http\Controllers;

use Illuminate\Http\Request;

class ContatoController extends Controller
{
    public function contato(Request $request) {
        echo '<pre>';
        print_r($request->all());
        echo '</pre>';
        return view('site.contato', ['titulo' => 'Contato (teste)']);
    }
}
~~~

- ao invés de recuperar todos os dados com **all()**, podemos também utilizar o método **input()** para recuperar um dado específico, como no exemplo:

~~~php
<?php

namespace App\Http\Controllers;

use Illuminate\Http\Request;

class ContatoController extends Controller
{
    public function contato(Request $request) {
        echo '<pre>';
        print_r($request->all());
        echo '</pre>';
        echo $request->input('nome');
        echo '<br>';
        echo $request->input('email');
        return view('site.contato', ['titulo' => 'Contato (teste)']);
    }
}
~~~

<h2 id="topico2">2. Gravando os dados do formulário no banco de dados</h2>

- a primeira coisa que precisamos fazer é trazer pro contexto do script o modelo que fará todo o encapsulamento através do Eloquent ORM com a tabela no banco - nesse caso, o `Model SiteContato`.

~~~php
<?php

namespace App\Http\Controllers;

use Illuminate\Http\Request;
use App\Models\SiteContato;

class ContatoController extends Controller
{
    public function contato(Request $request) {
        echo '<pre>';
        print_r($request->all());
        echo '</pre>';
        echo $request->input('nome');
        echo '<br>';
        echo $request->input('email');
    }
}
~~~

- em `ContatoController.php`:

~~~php
<?php

namespace App\Http\Controllers;

use Illuminate\Http\Request;
use App\Models\SiteContato;

class ContatoController extends Controller
{
    public function contato(Request $request) {

        $contato = new SiteContato();
        $contato->nome = $request->input('nome');
        $contato->telefone = $request->input('telefone');
        $contato->email = $request->input('email');
        $contato->motivo_contato = $request->input('motivo_contato');
        $contato->mensagem = $request->input('mensagem');
  
        // print_r($contato->getAttributes());

        $contato->save();

        return view('site.contato', ['titulo' => 'Contato (teste)']);
    }
}
~~~

- além desse modelo, há uma outra forma; em `ContatoController.php`:

~~~php
<?php

namespace App\Http\Controllers;

use Illuminate\Http\Request;
use App\Models\SiteContato;

class ContatoController extends Controller
{
    public function contato(Request $request) {

        $contato = new SiteContato();
        $contato->fill($request->all());
        $contato->save();

        return view('site.contato', ['titulo' => 'Contato (teste)']);
    }
}
~~~

- para que funcione o método acima, devemos ir no Model `SiteContato.php`:

~~~php
<?php

namespace App\Models;

use Illuminate\Database\Eloquent\Model;
use Illuminate\Database\Eloquent\Factories\HasFactory;

class SiteContato extends Model
{
    use HasFactory;
    
    protected $fillable = ['nome', 'telefone', 'email', 'motivo_contato', 'mensagem'];
}
~~~

> ao invés do método fill(), podemos utilizar o método create(), desde que adicionemos `protected $fillable`na model, como no exemplo abaixo; no arquivo `ContatoController.php`:

~~~php
<?php

namespace App\Http\Controllers;

use Illuminate\Http\Request;
use App\Models\SiteContato;

class ContatoController extends Controller
{
    public function contato(Request $request) {

        $contato = new SiteContato();
        $contato->create($request->all());

        return view('site.contato', ['titulo' => 'Contato (teste)']);
    }
}
~~~

<h2 id="topico3">3. Validação de campos obrigatórios (required)</h2>

- como validar os dados do formulário do lado do backend da aplicação?
- a validação de dados é muito importante para garantir o correto funcionamento da lógica do sistema, visto que dados obrigatórios (ou que devem possuir alguma característica específica) precisam ser validados ANTES de serem submetidos para outros processos dentro do sistema.
- neste momento, temos apenas um formulário dentro da aplicação, que é o `form_contato.blade.php`, que pode ser acessado através da Home ou da rota Contatos.

### a) Ajustando o código:

- em `routes` > `web.php`:

~~~php
<?php

use Illuminate\Support\Facades\Route;

Route::get('/contato', [\App\Http\Controllers\ContatoController::class,'contato']) -> name('site.contato');
Route::post('/contato', [\App\Http\Controllers\ContatoController::class,'salvar']) -> name('site.contato');
~~~

- em `app`> `Http` > `Controllers` > `ContatoController.php`:

~~~php
<?php

namespace App\Http\Controllers;

use Illuminate\Http\Request;
use App\Models\SiteContato;

class ContatoController extends Controller
{
    public function contato(Request $request) {

        // $contato = new SiteContato();
        // $contato->create($request->all());

        return view('site.contato', ['titulo' => 'Contato (teste)']);
    }

    public function salvar(Request $request) {
        SiteContato::create($request->all());

    }
}
~~~

### b) Validando os dados do formulário:

- há alguns detalhes importantes:
    - 1. no contexto das **Views** temos uma variável chamada `$errors`, que é disponibilizada pelo Laravel de modo automático para qualquer View; ela será afetada pelo nosso Controlador, então devemos determinar como lidar com essa variável.
    - 2. no nosso formulário, as colunas **nome**, **telefone**, **email**, **motivo_contato**, **mensagem**, **updated_at** e **created_at** são de preenchimento obrigatório!
        - portanto, antes de fazer a gravação com `SiteContato::create($request->all());`, devemos realizar a validação dos dados do formulário recebidos no request.

1. em `resources` > `views` > `site` > `layouts` > `_components` > `form_contato.blade.php`:

~~~php
<pre>
{{ print_r($errors) }}
</pre>
~~~

- obteremos o retorno:

~~~
Illuminate\Support\ViewErrorBag Object
(
    [bags:protected] => Array
        (
        )

)
1
~~~

2. iniciando a validação dos dados do formulário em `app`> `Http` > `Controllers` > `ContatoController.php`:
    - na validação, a chave utilizada será o name do input!

~~~php
public function salvar(Request $request) { 
        $request->validate([
            'nome' => 'required'
        ]);
    }
~~~

- nesse caso, será requerida a inserção do nome; caso não seja realizada, a variável $errors retornará:

~~~
    Illuminate\Support\ViewErrorBag Object
(
[bags:protected] => Array
    (
        [default] => Illuminate\Support\MessageBag Object
            (
                [messages:protected] => Array
                    (
                        [nome] => Array
                            (
                                [0] => The nome field is required.
                            )

                    )

                [format:protected] => :message
            )

    )

)
1
~~~

3. incluindo required para todos os inputs de `form_contato.blade.php`, pois todos os campos do BD são obrigatórios:

~~~php
public function salvar(Request $request) {
    $request->validate([
        'nome' => 'required',
        'telefone' => 'required',
        'email' => 'required',
        'motivo_contato' => 'required',
        'mensagem' => 'required'
    ]);
}
~~~

<h2 id="topico4">4. Validação de quantidades mínimas e máximas de caracteres (min e max)</h2>

- na documentação do Laravel, acessar The basics > [Validation](https://laravel.com/docs/13.x/validation) > [Available Validation Rules](https://laravel.com/docs/13.x/validation#available-validation-rules).
    - [Required](https://laravel.com/docs/13.x/validation#rule-required).
    - [Min](https://laravel.com/docs/13.x/validation#rule-min).
    - [Max](https://laravel.com/docs/13.x/validation#rule-max).

- em `app`> `Http` > `Controllers` > `ContatoController.php`:
    - nomes com mínimo de 3 caracteres e máximo de 40 caracteres.

~~~php
public function salvar(Request $request) {
    
    $request->validate([
        'nome' => 'required | min:3 | max:40'
    ]);
}
~~~

- erros exibidos:
    - se deixar o campo nome vazio: "The nome field is required."
    - se preencher com menos de 3 caracteres: "The nome field must be at least 3 characters."
    - se preencher com mais de 40 caracteres: "The nome field must not be greater than 40 characters."

- ajustar também o imput mensagem:

~~~php
public function salvar(Request $request) {
    $request->validate([
        'nome' => 'required | min:3 | max:40',
        'telefone' => 'required',
        'email' => 'required',
        'motivo_contato' => 'required',
        'mensagem' => 'required | max:2000'
    ]);
}
~~~

<h2 id="topico5">5. Repopulando o formulário (Request Old Input) parte 1</h2>

- `Request Old Input` permite recuperar os dados que foram preenchidos no formulário, ainda que haja um erro de validação.
- o motor de renderização blade possui no seu contexto a `função old()`, a qual recebe por parâmetro o nome de determinado input, para ter acesso à informação que foi preenchida em uma requisição anterior (antes do redirect feito pelo próprio framework Laravel em caso de erro).

- em `resources` > `views` > `site` > `layouts` > `_components` > `form_contato.blade.php`, incluir o **atributo value**:
    - obs: no caso do select, a tratativa é diferenciada, será abordada na próxima aula.

~~~php
<form action={{ route('site.contato') }} method="post">
    @csrf
    <input name="nome" value="{{ old('nome') }}" type="text" placeholder="Nome" class="{{ $classe }}">
    <br>
    <input name="telefone" value="{{ old('telefone') }}" type="text" placeholder="Telefone" class="{{ $classe }}">
    <br>
    <input name="email" value="{{ old('email') }}" type="text" placeholder="E-mail" class="{{ $classe }}">
    <br>
    <select name="motivo_contato" class="{{ $classe }}">
        <option value="">Qual o motivo do contato?</option>
        <option value="1">Dúvida</option>
        <option value="2">Elogio</option>
        <option value="3">Reclamação</option>
    </select>
    <br>
    <textarea name="mensagem" class="{{ $classe }}">
        @if(old('mensagem') != '')
            {{ old('mensagem') }}
        @else
            "Preencha aqui a sua mensagem!"
        @endif
    </textarea>
    <br>
    <button type="submit" class="{{ $classe }}">ENVIAR</button>
</form>
~~~

<h2 id="topico6">6. Repopulando o formulário (Request Old Input) parte 2</h2>

- em `resources` > `views` > `site` > `layouts` > `_components` > `form_contato.blade.php`:

~~~php
<select name="motivo_contato" class="{{ $classe }}">
    <option value="">Qual o motivo do contato?</option>
    <option value="1" {{ old('motivo_contato') == '1' ? 'selected' : '' }}>Dúvida</option>
    <option value="2" {{ old('motivo_contato') == '2' ? 'selected' : '' }}>Elogio</option>
    <option value="3" {{ old('motivo_contato') == '3' ? 'selected' : '' }}>Reclamação</option>
</select>
~~~

- porém, na prática, **não utilizamos as opções do select dessa forma**. Na prática, os options de um select são recuperados do banco, e são preenchidos na view através de um foreach.
    - portanto, em `app`> `Http` > `Controllers` > `ContatoController.php`, criaremos um array no método ontato, simulando que a origem das informações teria sido o banco de dados.

~~~php
public function contato(Request $request) {

    $motivo_contatos = [
        1 => 'Dúvida',
        2 => 'Elogio',
        3 => 'Reclamação'
    ];

    return view('site.contato', ['titulo' => 'Contato (teste)', 'motivo_contatos' => $motivo_contatos]);
}
~~~

- em `resources` > `views` > `site` > `contato.blade.php`:

~~~php
<div class="informacao-pagina">
    <div class="contato-principal">
        @component('site.layouts._components.form_contato', ['classe' => 'borda-preta', 'motivo_contatos' => $motivo_contatos])
        <p>A nossa equipe analisará a sua mensagem e retornaremos o mais brevemente possível!</p>
        <p>Nosso tempo médio de resposta é de 48 horas.</p>
        @endcomponent
    </div>
</div>  
~~~

- em `resources` > `views` > `site` > `layouts` > `_components` > `form_contato.blade.php`:

~~~php
<select name="motivo_contato" class="{{ $classe }}">
    <option value="">Qual o motivo do contato?</option>
    @foreach($motivo_contatos as $key => $motivo_contato)
        <option value="{{ $key }}" {{ old('motivo_contato') == $key ? 'selected' : '' }}>{{ $motivo_contato }}</option>
    @endforeach
</select>
~~~

<h2 id="topico7">7. Ajustando o formulário de contato na rota principal</h2>

- em `app`> `Http` > `Controllers` > `ContatoController.php`, copiar o array e colar em `PrincipalController.php`:

~~~php
$motivo_contatos = [
    1 => 'Dúvida',
    2 => 'Elogio',
    3 => 'Reclamação'
];
~~~

- em `resources` > `views` > `site` > `principal.blade.php`:

~~~php
<div class="contato">
    <h1>Contato</h1>
    <p>Caso tenha qualquer dúvida por favor entre em contato com nossa equipe pelo formulário abaixo.<p>
    @component('site.layouts._components.form_contato', ['classe' => 'borda-branca', 'motivo_contatos' => $motivo_contatos])
    @endcomponent
</div>
~~~

<h2 id="topico8">8. Refactoring do projeto Super Gestão parte 1</h2>

- há repetição de código em `ContatoController.php` e `PrincipalController.php`; criado de modo estático e em duplicidade, como no exemplo abaixo:

~~~php
$motivo_contatos = [
    1 => 'Dúvida',
    2 => 'Elogio',
    3 => 'Reclamação'
];
~~~

- desenvolveremos um Model MotivoContato, no qual criaremos uma migration e uma tabela no banco de modo incremental! Portanto, na linha de comando:

~~~
$ php artisan make:model MotivoContato -m
~~~

- em `Models` > `MotivoContato.php`:

~~~php
<?php
namespace App\Models;
use Illuminate\Database\Eloquent\Model;
class MotivoContato extends Model
{
    protected $fillable = ['motivo_contato'];
}
~~~

- em `database` > `migrations` > `create_motivo_contatos_table.php`:

~~~php
public function up(): void
{
    Schema::create('motivo_contatos', function (Blueprint $table) {
        $table->id();
        $table->string('motivo_contato', length: 20);
        $table->timestamps();
    });
}
~~~

- para popular a tabela, criaremos um Seeder:

~~~
php artisan make:seeder MotivoContatoSeeder
~~~

- em `database` > `seeders` > `DatabaseSeeder.php`:

~~~php
public function run(): void
    {
        $this->call(FornecedorSeeder::class);
        $this->call(SiteContatoSeeder::class);
        $this->call(MotivoContatoSeeder::class);
    }
~~~

- em `database` > `seeders` > `MotivoContatoSeeder.php`:

~~~php
public function run(): void
{
    MotivoContato::create(['motivo_contato' => 'Dúvida']);
    MotivoContato::create(['motivo_contato' => 'Elogio']);
    MotivoContato::create(['motivo_contato' => 'Reclamação']);
}
~~~

- executar o comando:

~~~
php artisan migrate:status
php artisan migrate
~~~

- executar a classe Seeder, através da instrução:

~~~
php artisan db:seed --class=MotivoContatoSeeder
~~~

- para recuperar essas informações nos Controladores, em `PrincipalController.php`:

~~~php
use App\Models\MotivoContato;

class PrincipalController extends Controller
{
    public function principal() {
        $motivo_contatos = MotivoContato::all();
    
        return view('site.principal', ['motivo_contatos' => $motivo_contatos]);
    }
}
~~~

- em `form_contato.blade.php`:

~~~php
<select name="motivo_contato" class="{{ $classe }}">
    <option value="">Qual o motivo do contato?</option>
    @foreach($motivo_contatos as $key => $motivo_contato)
        <option value="{{ $motivo_contato->id }}" {{ old('motivo_contato') == $motivo_contato->id ? 'selected' : '' }}>{{ $motivo_contato->motivo_contato }}</option>
    @endforeach
</select>
~~~

- adicionar a mesma instrução em `ContatoController.php`:

~~~php
use App\Models\MotivoContato;

class ContatoController extends Controller
{
    public function contato(Request $request) {

        $motivo_contatos = MotivoContato::all();

        return view('site.contato', ['titulo' => 'Contato (teste)', 'motivo_contatos' => $motivo_contatos]);
    }
}
~~~

<h2 id="topico9">9. Refactoring do projeto Super Gestão parte 2</h2>

